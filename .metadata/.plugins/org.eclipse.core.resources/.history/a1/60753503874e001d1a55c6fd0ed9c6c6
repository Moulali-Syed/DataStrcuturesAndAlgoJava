package qOOPS1;

public class FConstructors {

	/*
	 When we see 
	 new Scanner(System.in);
	 new Student();
	 
	 both looks similar to function call, one accepts an argument other is not
	 
	 They are actually constructors
	 
	 Constructor: is similar to function without return type, will be called once
	 				in a objects lifetime
	 				
	 We have default constructor for all the classes that we create
	 */
}
