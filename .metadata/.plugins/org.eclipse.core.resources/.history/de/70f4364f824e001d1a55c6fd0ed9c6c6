package qOOPS1;

public class CAccessModifiers {
//Access modifiers restrict the access
	/*
	 public
	 private
	 default
	 
	 if we want to have a property read and write within class , then use private 
	 private - access only with in class
	 
	 if no access modifier is specified , then it is default access modifier
	 can be used within a package
	 */
}
